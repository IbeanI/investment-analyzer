# Investment Portfolio Analyzer - Docker Environment Variables
#
# SETUP:
#   1. Copy this file to .env:  cp .env.example .env
#   2. Replace placeholder values with your own
#
# These variables are used by docker-compose.yml to configure services.

# =============================================================================
# PostgreSQL Database Configuration
# =============================================================================

# Database credentials (used by both PostgreSQL container and backend)
POSTGRES_USER=your_db_user
POSTGRES_PASSWORD=your_secure_password_here
POSTGRES_DB=investment_portfolio

# Optional: For local development scripts that connect directly
POSTGRES_HOST=localhost
POSTGRES_PORT=5432

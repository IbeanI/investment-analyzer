# Investment Portfolio Analyzer - Backend Environment Variables
#
# SETUP:
#   1. Copy this file to .env:  cp .env.example .env
#   2. Replace placeholder values with your own
#
# These variables are used by the FastAPI application.

# =============================================================================
# Application Settings
# =============================================================================

# Enable debug mode (more verbose logging)
DEBUG=false

# Logging level: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# =============================================================================
# Database Connection
# =============================================================================

# Connection string for PostgreSQL
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE
#
# Replace with your actual credentials:
DATABASE_URL=postgresql://your_db_user:your_secure_password_here@localhost:5432/investment_portfolio

# Note: When running inside Docker, the DATABASE_URL is set automatically
# by docker-compose.yml using the container hostname (investment_db).
